  <div class="container">
    <p id="notice"><%= notice %></p>
    <h1><%= @garment.name %></h1>

    <div class="row">
      <%= form_tag proceed_orders_path, method: :post, id: "proceed_to_order_form" do %>
      	<div class="col-md-12">
          <div class="panel-group" id="accordion"> 
            
            <%= render partial: "parts/part", collection: parts_position(@garment.parts) %>
          
          </div>
        </div>
           <%= hidden_field_tag :garment_id , @garment.id %>
      <% end %>
    </div>
    <div class="row">
      <div class="col-md-12"><button type="button" class="btn btn-primary" id="proceed_to_order">Proceed</button></div>
    </div>
  </div>
<%#so in the show need to keep the records of mismatch values%>
<%#and if that happens then make it dash%>

 <!--
 <input type="text" name="previousclass" id="previousclass" value="" style="display:none">
 currently not needed
 
 
 <!--
 <input type="text" name="previousdashedid" id="previousdashedid" value="" style="display:none">
 -->
 <div id="herelocktextboxkept" style="display:none">
   
 </div>