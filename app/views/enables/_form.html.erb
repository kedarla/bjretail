<%= form_for(@enable) do |f| %>
  <% if @enable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enable.errors.count, "error") %> prohibited this enable from being saved:</h2>

      <ul>
      <% @enable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :option_id %><br>
    <%= f.number_field :option_id %>
  </div>
  <div class="field">
    <%= f.label :enable_element_id %><br>
    <%= f.number_field :enable_element_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
