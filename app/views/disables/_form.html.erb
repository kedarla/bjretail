<%= form_for(@disable) do |f| %>
  <% if @disable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@disable.errors.count, "error") %> prohibited this disable from being saved:</h2>

      <ul>
      <% @disable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :option_id %><br>
    <%= f.number_field :option_id %>
  </div>
  <div class="field">
    <%= f.label :disable_element_id %><br>
    <%= f.number_field :disable_element_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
